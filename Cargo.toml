[package]
name = "clipboarder"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
clipboard-win = { version = "5.3.0", features = ['monitor'] }
regex = "1.10.4"
